<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>New Question</title>
</head>
<body>

  <%= form_for @question do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :text %>
  <%= f.text_area :text %>

  <%= f.submit "Add question" %>
  <% end %>

</body>
</html>
